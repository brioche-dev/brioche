[workspace]
members = ["crates/brioche", "crates/brioche-pack"]
default-members = ["crates/brioche"]
resolver = "2"

[profile.release-tiny]
inherits = "release"
opt-level = "z"
lto = true
codegen-units = 1
strip = "symbols"
panic = "abort"
